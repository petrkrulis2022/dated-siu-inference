{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "receipt.schema.json",
  "title": "Receipt",
  "description": "verify_receipt's signed attestation: quoted vs paid vs matched — build1-spec.md §9.",
  "type": "object",
  "additionalProperties": false,
  "definitions": {
    "decimalString": {
      "type": "string",
      "pattern": "^-?[0-9]+(\\.[0-9]+)?$",
      "description": "A decimal number encoded as a string. Never a JSON number — no floats in money maths."
    },
    "hexString": {
      "type": "string",
      "pattern": "^0x[0-9a-fA-F]+$"
    }
  },
  "properties": {
    "schema_version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$",
      "description": "Build 1 emits \"1.0\". A same-major bump is additive and safe for an existing consumer to accept; a major bump is not."
    },
    "quote_hash": {
      "type": "string",
      "pattern": "^0x[0-9a-fA-F]{64}$",
      "description": "bytes32 — matches TouchstoneEscrow.openAndFund's quoteHash param, build1-spec.md §10."
    },
    "chain": {
      "type": "string",
      "minLength": 1
    },
    "tx_ref": {
      "type": "string",
      "minLength": 1
    },
    "amount_quoted_usd": { "$ref": "#/definitions/decimalString" },
    "amount_paid_usd": {
      "$ref": "#/definitions/decimalString",
      "description": "Gross: the actualAmount passed to TouchstoneEscrow.settle, before the feeBps treasury split — build1-spec.md §10."
    },
    "matched": {
      "type": "boolean"
    },
    "print_ref": {
      "type": "string",
      "minLength": 1
    },
    "signature": { "$ref": "#/definitions/hexString" },
    "public_key": { "$ref": "#/definitions/hexString" }
  },
  "required": [
    "schema_version",
    "quote_hash",
    "chain",
    "tx_ref",
    "amount_quoted_usd",
    "amount_paid_usd",
    "matched",
    "print_ref",
    "signature",
    "public_key"
  ]
}
