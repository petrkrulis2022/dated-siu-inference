/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * The declared, authoritative list of run-record files that constitute one print — written once at publish time to data/runs/<print_id>/index.json. A reader must load run records by this list, never by listing the directory: the directory can hold leftover files from an earlier failed attempt under the same print_id (a same-day retry runs into the same directory), and only this manifest states which files were actually used to compute the print.
 */
export interface RunManifest {
  print_id: string;
  /**
   * Basket version, e.g. SIU-2026a — matches Print.version for the same print.
   */
  basket_version: string;
  methodology_version: string;
  /**
   * Filenames only (e.g. "<run_id>.json"), resolved relative to this manifest's own directory. Sorted, so the file is stable and diffable.
   */
  run_records: string[];
}
