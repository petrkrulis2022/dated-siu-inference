{
  "name": "@datum/print",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "compute": "pnpm run build && node dist/cli/compute.js",
    "verify": "pnpm run build && node dist/cli/verify.js",
    "publish-print": "pnpm run build && node dist/cli/publish.js"
  },
  "dependencies": {
    "@datum/basket": "workspace:*",
    "@datum/harness": "workspace:*",
    "@datum/sdk": "workspace:*",
    "@noble/curves": "^2.3.0",
    "@noble/hashes": "^2.3.0",
    "canonicalize": "^4.0.0",
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
