{
  "name": "@touchstone/print",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "compute": "pnpm run build && node dist/cli/compute.js",
    "verify": "pnpm run build && node dist/cli/verify.js",
    "publish-print": "pnpm run build && node dist/cli/publish.js",
    "verify-onchain": "pnpm run build && node dist/cli/verify-onchain.js"
  },
  "dependencies": {
    "@touchstone/basket": "workspace:*",
    "@touchstone/harness": "workspace:*",
    "@touchstone/sdk": "workspace:*",
    "@noble/curves": "^2.3.0",
    "@noble/hashes": "^2.3.0",
    "canonicalize": "^4.0.0",
    "decimal.js": "^10.6.0",
    "viem": "^2.55.16"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
