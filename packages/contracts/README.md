# @datum/contracts

Solidity: `DatumAttestation`, `DatumEscrow` (Foundry, Base).

Not a pnpm workspace member — separate toolchain, per CLAUDE.md's stack conventions. Foundry
scaffold (`foundry.toml`, `src/`, `test/`, `script/`) arrives with the contracts milestone, not
in this scaffold pass.
